{
  "name": "inneranimalmedia-mcp",
  "description": "Inner Animal Media MCP server — company-wide CI/CD for 30+ Workers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "echo 'No tests configured'",
    "migrate:telemetry": "./scripts/apply-telemetry-migration.sh",
    "db:query": "wrangler d1 execute inneranimalmedia-business --command",
    "db:file": "wrangler d1 execute inneranimalmedia-business --file"
  },
  "dependencies": {
    "agents": "^0.3.6",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
